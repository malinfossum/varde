# OS junk
Thumbs.db
.DS_Store

# Editor
.vs/
.idea/
.vscode/
.zed/
.claude/
*.user
*.suo

# .NET build output
bin/
obj/
publish/
*.publishsettings

# Secrets — never tracked. Connection strings live in dotnet user-secrets.
appsettings.Development.json
appsettings.*.Local.json
.env
.env.*
# ...but the contract for which variables the stack needs is in Git
!.env.example
*.pfx

# Node (web/ arrives in plan 2)
node_modules/
dist/
.vite/

# Logs
*.log

# Brainstorming mockups (visual companion) — the sdd ledger next to it stays tracked
.superpowers/brainstorm/
