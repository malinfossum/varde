// Sibling declaration for sync-fonts.mjs — this project carries no allowJs/@types/node, so a
// plain .mjs import has no type on its own. tests/fonts.test.ts is the only TS consumer.
export declare const FONT_FILES: [file: string, pkg: string][]
