# Copy to .env and fill in real values: cp .env.example .env
# .env stays out of Git; this file is the contract for which variables the stack needs.

POSTGRES_DB=varde
POSTGRES_USER=varde
POSTGRES_PASSWORD=change-me
